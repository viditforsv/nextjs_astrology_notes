# Auth Configuration
# Generate with: openssl rand -base64 32
AUTH_SECRET=your-secret-key-here-generate-with-openssl-rand-base64-32

# Google OAuth Credentials
# Get these from: https://console.cloud.google.com/apis/credentials
# IMPORTANT: Add these Authorized redirect URIs in Google Cloud Console:
#   - http://localhost:3000/api/auth/callback/google (for development)
#   - https://astrology.viditaggarwal.com/api/auth/callback/google (for production)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Allowed Users (comma-separated email addresses)
# Only users with these emails will be able to sign in
# Leave empty to allow all users
ALLOWED_USERS=user1@example.com,user2@example.com

